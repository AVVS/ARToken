{
  "devDependencies": {
    "@digix/web3-console": "^0.0.2",
    "bluebird": "^3.5.1",
    "ethereumjs-abi": "^0.6.4",
    "ganache-cli": "^6.0.3",
    "ganache-core": "^2.0.2",
    "lodash": "^4.17.4",
    "papaparse": "^4.3.6",
    "truffle": "^4.0.1",
    "web3": "0.x.x"
  },
  "scripts": {
    "develop": "truffle develop",
    "compile": "truffle compile",
    "rpc": "ganache-cli --host 127.0.0.1 -l 115000000 -d -i 1337",
    "t:test": "truffle --network test",
    "t:ropsten": "NODE_ENV=production truffle --network ropsten",
    "t:live": "NODE_ENV=production truffle --network live",
    "deploy": "yarn compile && yarn t:test deploy",
    "deploy:ropsten": "rm -rf ./build; NODE_ENV=production yarn compile && yarn t:ropsten deploy",
    "deploy:live": "rm -rf ./build; NODE_ENV=production yarn compile && yarn t:live deploy",
    "parity:ropsten": "parity ui --force-ui --chain ropsten --geth --jsonrpc-port 8547 --reseal-min-period 0 --tx-gas-limit 4704624 --tx-queue-mem-limit 100",
    "parity:live": "parity ui --force-ui --geth --jsonrpc-port 8548 --reseal-min-period 0 --tx-gas-limit 4704624 --tx-queue-mem-limit 100",
    "e:ropsten": "yarn t:ropsten exec"
  },
  "resolutions": {
    "web3": "0.20.2"
  }
}
